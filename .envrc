# Auto-load the dev shell on `cd` (needs direnv + nix-direnv; run `direnv allow`).
# Without direnv, use `nix develop` instead.
use flake

# Local, gitignored secrets, if any. Not needed for build or the
# unit/arch suite.
dotenv_if_exists .env
